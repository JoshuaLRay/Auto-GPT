{
  "name": "dustin-yelton-website",
  "version": "1.0.0",
  "description": "Dustin Yelton Painting & Cleaning — Eleventy site with Sveltia CMS admin",
  "private": true,
  "scripts": {
    "build": "eleventy",
    "dev": "eleventy --serve",
    "deploy": "npm run build && wrangler deploy",
    "preview": "npm run build && wrangler dev"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0",
    "wrangler": "^4.100.0"
  }
}
